/**
 * Browser event used to open the global calculator command palette from UI
 * (e.g. header search button). Must match the listener in `CommandMenu.tsx`.
 */
export const OPEN_CALCULATOR_COMMAND_MENU_EVENT = 'open-calculator-command-menu';
